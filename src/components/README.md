# Components

Reusable, domain-neutral UI primitives live in `ui/`; feature-owned components remain with their feature. Import primitives directly from their module rather than introducing a barrel file so Vite can keep feature bundles explicit.

See [the Phase 16 design-system guide](../../docs/phase-16-design-system.md) for component roles, tokens, accessibility, and responsive behavior.
